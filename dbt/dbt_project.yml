name: btc_pipeline
version: '1.0.0'
config-version: 2

profile: btc_pipeline

model-paths: ["models"]
test-paths: ["tests"]

models:
  btc_pipeline:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts